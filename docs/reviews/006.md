- Expose types via some framework so that people can work with TypeScript as well, and are not locked to JavaScript only.
- Fan-out pull is taking a lot of time.
- How do alerts work in MDK?
- Is it possible to have a UI generated solely based on the contract and plugin JSON, without requiring manual code?
- On bootstrap of the UI Shell, what should it render? If it displays some UI, does the required API for it also come out of the box from the backend bootstrap, and is it generic for all types of workers/gateway plugins? @Hemant @Arif_Dewi